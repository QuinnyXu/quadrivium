# Override Log

When the Maintainer overrides an agent verdict, log it here with one paragraph: what was proposed, what was overridden, the reasoning, and the kill condition that would prove the override wrong. Periodic panel reviews check overrides against outcomes — that is where the calibration happens, not in-session.

Format: date, agent overridden, proposed bet, verdict, override, kill condition.

---

- **2026-05-30 — Decision B (concept-DOI-per-dataset) OVERTURNED; Vision (c) overridden.**
  - *Agent overridden:* Vision (the v1.0.0-fedsupport deposit-DOI-shape step-1 consultation).
  - *Proposed bet (Vision):* shape **(c)** — deposit fedsupport as a standalone **manual** Zenodo record with its own pre-reserved concept DOI, honoring Decision B (separate concept DOI per dataset) and §10 (one repo) by bending the deposit *mechanism* to manual upload. Vision reasoned at the **survey-as-citable-unit** level, and on that level (c) was correct (the only path honoring both locked doctrines).
  - *Override (Maintainer):* reframe one level up, at **product identity** — the citable unit is the **integrated harmonized database, NOT the individual survey**. ONE repo → ONE concept DOI (`10.5281/zenodo.20404785`) for the whole database; datasets are subtrees; each GitHub release mints a **version DOI** under the one concept via the standard Zenodo integration. This **OVERTURNS Decision B** (it is an overturn, not an amendment). It also dissolves the problems (c) was solving: no manual deposit, no pre-reservation inversion, no automation loss, and the cross-survey spine gets an unambiguous home (the database).
  - *Reasoning:* (1) **thesis-is-relatedness** — the product IS the integrated whole; the cross-survey relatedness (the join spine especially) is the value, and per-dataset DOIs fragment exactly that relatedness and *orphan* the cross-survey spine (no per-survey deposit can own a relationship-level artifact); (2) the **verified Zenodo fact-find** (2026-05-30) — per-dataset DOIs would have forced **permanent manual deposit** (the GitHub integration nests new releases as versions under the repo's own concept DOI and cannot auto-create a second standalone concept for the same repo), a standing operational tax the reversal eliminates. Vision's (c) was well-reasoned *below* the reframe; the override is at product-identity, not a correction of Vision's logic at its own level.
  - *Kill condition (what would prove the overturn wrong):* if a future dataset needs **independent citation, independent licensing, or an independent release cadence** the single-concept model cannot express — e.g. a survey citable/versioned on its own clock decoupled from the database, or a survey whose license/embargo differs from the database's CC-BY-4.0 such that bundling under one DOI misrepresents its terms — then the one-concept model is too coarse and per-dataset (or a hybrid: database concept + satellite concepts for divergent-license surveys) must be revisited. While every survey is CC-BY-4.0, NCSES-sourced, and released on the database cadence, the overturn holds.

- **2026-05-31 — Decision record (not a doctrine override): dataset-#3 selection — principal's initial A-then-B lean updated to B-then-A on verified evidence.** (Distinct in kind from the 2026-05-30 entry above, which *was* a doctrine override — it reversed a locked decision, per-dataset DOIs. This entry is a forward selection in which the principal updated their *own* initial lean on evidence; no agent verdict or locked doctrine is overridden. Logged in override-log format — decision + reasoning + kill condition — so the divergence-from-lean and its evidentiary basis are durable, without conflating a lean-update with a doctrine reversal.)
  - *Decision:* **dataset #3 = NSF GSS** (Survey of Graduate Students and Postdoctorates in S&E), the human-capital / Tier-1 face of the funding→productivity thread; **GSS-first, SED deferred** (person-grain, does not join the institution-identity spine natively). **Dataset #4 = IPEDS IC snapshot** (the class-(a) spine backfill). This is **B-then-A**: third analytical face first, authoritative-identity bridge second.
  - *Initial lean (principal):* **A-then-B** — IPEDS IC first, to make the cross-survey spine authoritative (UNITID registry) before adding the third face, on the reasoning that a once-fixed foundation pays forward to GSS and every future dataset, and that B's main advantage (a visible milestone) is worth ~0 with no Stage-1 audience yet.
  - *Divergence (Vision, ratified):* the lean's load-bearing premise — that IPEDS IC (Candidate A) "attacks the exact cost that capped both deposits" — is **half-true**. Confirmed against `validation/reports/fedsupport/identity_spine_match_rate.md`: the dollar-match gap decomposes into THREE named structural classes, and A fixes only ONE — **(a) 7.9% / $3.86B HERD-side NULL-`ipeds_unitid`** (gate-check (c): 19 of 22 institutions genuinely NULL at the raw NSF source; an authoritative UNITID registry backfills these, lifting dollar-match 73.1%→~80.7%, then stopping). The other two are **immune to an identity registry**: **(b) 7.6% / $3.73B system-vs-campus grain seam** (an RH clause-(b) decomposition object, HD 3.6 Seam B — a registry cannot pick the campus for a system-level obligation) and **(c) 10.8% / $5.29B no-HERD-name** (out-of-HERD-universe by construction — a better bridge cannot manufacture a HERD row that does not exist). So A does LESS than the architecture-first lean assumed: it buys back the smaller half of the gap and lands at the already-characterized ~80.7% best-case ceiling.
  - *Deciding reasoning (why GSS first):* GSS's S&E scope carries a **funding-mechanism dimension**, making it the **funding-of-human-capital** face that completes the three-face thesis (funding-IN → funding-OUT → what-it-produces, §1). At Stage 1 with foundation-quality (not visible milestones) as the driver, completing the thesis is the **larger foundation act** than buying back ~7 points of dollar-match on a two-face database. Secondary B-first benefit (requirements discovery): GSS is the third consumer the authoritative spine (dataset #4) must serve, so building it first surfaces #4's full requirements — especially whether GSS's departmental/field grain stresses the class-(b) seam differently than FedSupport's dollar obligations — *before* the bridge locks its spec; B-first de-risks A's design rather than merely delaying it.
  - *Alternative considered and DECLINED during the NSF outage:* **IPEDS Fall Enrollment** — available (IPEDS, not behind the down NSF site) and joins the spine natively, so a convenient unblocked substitute. Declined: it is an **enrollment/demographic face with NO funding dimension** — tangential to the funding→productivity thread, not a substitute for GSS's funding-of-human-capital face. Principal chose to **WAIT for the thesis-correct dataset** over the convenience substitution.
  - *Blocker + trigger:* GSS acquisition is **blocked on NSF/NCSES survey-data availability** (down as of 2026-05-31, no resumption schedule, per principal in-browser check). **NEXT ACTION ON UNBLOCK:** scope + run the **HD 4.1 GSS gate-spike** — RH era-boundary decomposability across the late-2000s GSS methodology/coverage discontinuity + the two-number (institution + count/dollar axis) spine-join receipt + the class-(b) requirements-discovery check (does GSS's grain hit the seam differently than FedSupport) + the §3 xlsx→CSV acquisition-format check. No spike scoping now — pointless against an unreachable source; resumption is the unambiguous trigger.
  - *Kill condition (what would prove the divergence wrong):* if the GSS build later forces an amendment to dataset-#4's authoritative-bridge spec that an A-first ordering would already have locked — i.e., GSS surfaces NO new spine requirement and the bridge A would have built is unchanged by GSS's existence — then B-first bought no requirements-discovery value, and the lean's A-then-B (fix the foundation before adding the face) was the better sequencing after all.

- **2026-06-01 — Decision record: FedSupport full-series re-base AUTHORIZED (b/c/d/e/g/h), decision (f) SENT BACK.** (Decision record, not a doctrine override — the maintainer accepted the agent scope recommendation and corrected one sub-finding on reviewer evidence.)
  - *Decision:* the re-base scope memo (`docs/fedsupport_rebase_scope.md`) is accepted; Phase 3 built on the full-series NCSES Build Table export (FY1971–FY2023, 434,547 rows). Ratified: **(b)** Build Table export supersedes Table 12 (→ audit siblings); **(c)** native IPEDS UnitID supersedes the name-reconstruction spine (name-match retired for FedSupport↔IPEDS; spine pivots to UNITID-keyed join + authoritative alias + HERD-backfill OFFER, offer-only/Seam-B-gated/gated to IPEDS #4); **(d)** full long-grain artifact + (fy,unitid) aggregate + R&D-broad isolation with S&E-support superset; **(e)** RFC-4180 loader + column-integrity assertion; **(g)** CSV-native provenance into `data/raw/MANIFEST.md`; **(h)** version `v3.0.0-fedsupport` under the one concept DOI.
  - *Sent back:* **(f)** discontinuity treatment — see the calibration finding below. The build pipeline did not depend on (f); the methods-note discontinuity section uses the corrected (f).
  - *Kill condition:* if the corrected (f) re-derivation or the native-UNITID join receipt fails to reproduce against published anchors, the supersede decisions (b)/(c) reopen. (They reconcile — FY2023 +47 kUSD; native-UNITID join 78.4% > MVP 73.1%.)

- **2026-06-02 — HD 4.3 decision record: GSS §5 ground-truth anchors + field-code reference named; SAS-cross-check superseded by the published anchor (Phase-A scope, maintainer-signed-off).** (Decision record, not an agent override. Scope: `docs/gss/hd_4_3_scope.md`. Phase A surfaced the acquisition needs and STOPPED for maintainer staging + authorization, preserving the gate→build checkpoint.)
  - *§5 anchor plan (maintainer stages — sandbox can't reach NSF, as with HERD Table 26 / FedSupport Table 12):* the GSS 2023 report **NSF 25-317** (`ncses.nsf.gov/pubs/nsf25317/table/{N}`). Support panel → **Tables 1-6** (primary source of support), **1-7** (detailed federal source by agency), **1-8** (mechanism), all longitudinal 1975–2023 — exact anchors because GSS support is *primary-source* (each FT student counted once; our fed+nonfed+self sum to the FT total). Race panel → **1-2a / 4-3 / 1-11a / 1-11b / 2-4**. PD_NFR panel → **1-9b / 3-2 / 3-4**. Target 2023 values from `gss_support.parquet`: federal 82,764 (NIH 23,172 / NSF 21,209 / DOD 9,171 / DOE 5,757 / USDA 3,332 / NASA 2,178 / HHS-other 3,314 / other 14,631), nonfederal 261,737, self 254,087; FT 598,588 / PT 219,507; postdocs 65,850.
  - *Field-code reference (maintainer stages):* GSS **Taxonomy of Disciplines (TOD) → IPEDS CIP** crosswalk mapping `gss_code` → field name + broad field — the technical-notes field tables (**A-17 / A-18a / A-18b**, current cycle) or the **GSS PUF codebook**. The GSS analogue of HERD `discipline_coarse/fine.csv`. (Auto-fetch of the 2021 PUF guide returned corrupt over the wire — the staged copy is authoritative.) **Gating:** if not staged promptly, the parquets build with `field_coarse/fine` NULL and **only** the field-code crosswalk slips; it must not block §5 reconciliation or the Race/PD_NFR builds.
  - *SAS↔XLSX disposition (extends the §3 pattern):* **the §5 published-total anchor SUPERSEDES the SAS↔XLSX byte cross-check; no `pyreadstat` at any stage** (acquisition included). Once a panel reconciles to the published NCSES totals (the authoritative ground truth), the SAS-vs-XLSX internal check is redundant (both are NCSES renderings; the published table is the higher anchor). The toolchain stays `pyreadstat`-free; the `sas7bdat` remains the unread provenance sibling. This **strengthens the HD 4.1 §3 ratification** (XLSX-authoritative) — the published anchor, not a SAS reader, is the correctness backstop for GSS and every future NCSES microdata-zip survey (SED).
  - *Sequencing (maintainer choice):* stage the references, then build all of Phase B in order (§5 anchor on Support first → Race + PD_NFR + their anchors → field-code crosswalk). No build started at Phase A.
  - *Kill condition:* if `gss_support.parquet` fails to reconcile to Tables 1-6/1-7/1-8 within a documented tolerance (e.g. rounding / suppressed-cell handling), the support-column crosswalk or the primary-source-semantics assumption reopens before the Race/PD_NFR builds proceed; if the staged field-code reference proves not to cover all 131 observed `gss_code` values, the uncovered codes default to `field_coarse/fine` NULL + footnote (codeset discipline, §4), not invented mappings.
  - **§5 verdict — HD 4.3 step 1 (the hard gate): PASS (2026-06-02).** `gss_support.parquet` reconciles against the staged NSF 25-317 anchors **exactly**: Table 1-7 federal total **49/49 years FY1975–2023, 0 mismatches**; 1-7 2023 by-agency all 8 cells exact; 1-6 2023 source split all 5 exact (our `nonfederal` = institutional + nonfed-domestic + foreign); 1-8 2023 fellowship/RA/TA/traineeship exact. The one apparent 1-8 "other" gap is a **documented GSS aggregation nuance** (our `support_mechanism='other'` 317,087 = published Other 63,000 + Self-support 254,087; GSS combines what 1-8 splits), not a discrepancy. The XLSX-authoritative §3 decision is now **validated against published ground truth** — the foundation all three GSS panels rest on. Receipt: `validation/reports/gss/gss_support_validation.md` §7. The staged anchor tables are SHA-pinned in `data/reference/MANIFEST.md` (read-once, never runtime-loaded). KC-1 (no clean key) and the XLSX-authoritative gate are both cleared; steps 2–3 (Race, PD_NFR) and step 4 (field codes) proceed.
  - **HD 4.3 steps 2–4 COMPLETE — all three GSS panels §5-PASS (2026-06-02/03).** **Step 2 `gss_race.parquet`** (12.66M rows): §5 PASS vs Tables 4-3/1-2a — all 8 cells exact (total 818,095; FT 598,588; first-time-FT 203,798; PT 219,507; male 422,321; female 395,774; master's 510,866; doctoral 307,229). **Step 3 `gss_pd_nfr.parquet`** (4.13M rows): §5 PASS vs Tables 1-9b/3-2/3-4/1-2a — all 16 cells exact (postdoc 65,850; NFR 34,342; source split; federal-by-agency NIH 18,732/NSF 3,853/…). PD_NFR is **overlapping marginal tables** → a `measure_group` discriminator (the support and demographic marginals independently total 65,850, confirming no double-count). Both panels: two-number spine receipt 97–99%/97.5–99.5%, 3-build deterministic. **Step 4 field codes (the dedicated TOD/PUF reference was NOT staged):** reconstructed `gss_code` → field via **count-matching** each code's 2023 all-grad total to the staged NSF 25-317 **Table 4-3** detailed-field rows — exact + unique for **all 91 2023-active codes** (`crosswalks/gss/field_code_map.csv`). `field_fine` = matched detailed name; `field_coarse` = the SEH super-broad (Science/Engineering/Health) parsed from Table 4-3's section structure. **Coverage 91/131 codes** (row-coverage 84.5%/83.5%/69.0% on support/race/pd_nfr; the 40 residual are historical-only codes not in the 2023 table). **Residual (per §4, no invented mappings):** the ~40 historical codes + the finer ~10-way broad subfields default to NULL pending the dedicated TOD/PUF reference (or earlier-year field tables). All three parquets rebuilt with `field_coarse/fine` joined (values UNCHANGED — §5 re-verified post-join; SHAs re-pinned in `data/harmonized/MANIFEST.md`). Validation reports: `validation/reports/gss/gss_{race,pd_nfr}_validation.md`. The GSS data layer (3 panels, all published-anchored) is complete; the methods note + RH boundary decompositions + v4 deposit remain the next increment.
  - **v4 deposit-prep Phase 1 — RH boundary decompositions + GSS methods note (2026-06-03; STOP for maintainer content review before Phase 2).** Maintainer decision: **ship v4 with the provisional count-match field-code, clearly labeled; TOD confirmation is a later patch, NOT a v4 blocker.** Three clause-(b) boundary decompositions (fixed-cohort method, HERD 2008→2011 template; `validation/reports/gss/gss_boundary_{2017,1984,2014}_decomposition.md`), each with the identity check passing: **2017 redesign** = net −35,713 is a *within-cohort* definitional drop (fixed-cohort −34,254 / frame only −1,459; the masters/doctoral split is additive, all_grad = ma+dr exactly); **1984–87** = a frame contraction/re-expansion (~190 institutions leave then return) masking +26,724 real cohort growth, net ~flat — the discontinuity is in *coverage*, not the enrollment total; **2014** = +33,576 expansion (real +19,215 / frame +14,361, incl. a ≤10,621 large-leaver UNITID-ambiguity residual). Clause-(c) residuals stated and bounded in each. **GSS methods note** (`docs/methods_notes/gss/reconstructive_harmonization_gss.md`, §9 cold-reader voice, lead-anchor: frame-instability problem-viz → decomposition-with-number → 49/49 validation receipt; 3 deterministic figures via the charts dev-group). Verbatim prose-vs-numbers read done (the v3 5th-calibration discipline) — caught + softened one overclaim ("not a real decline" → the defensible within-cohort-vs-frame split; real-vs-definitional inseparability kept as the body clause-(c) caveat). Honest disclosure in the note: field-code count-match-provisional (91/131 codes, field_coarse = S/E/H only, ~40 historical NULL pending TOD). No new runtime dep (matplotlib is charts dev-group). **STOP — content surfaced for maintainer review before the irreversible Phase-2 v4 mint.**
  - **v4 Phase 2 — deposit metadata + pre-tag gate (2026-06-03; STOP before the irreversible GitHub Release / Zenodo mint).** Maintainer authorized Phase 2 after content review (+ applied the §3 prose softening, and figure fixes landed: fig-3 title "definitional-dominated", fig-1 FY2024-tail annotated as latest-cycle-still-collecting, not a fourth discontinuity). Updated `.zenodo.json` + `README.md` + `CITATION.cff` to the **v4 THREE-survey integrated identity** (HERD expenditure-OUT + FedSupport funding-IN + **GSS human-capital**, joined on the institution-year hub; version 3.0.0→4.0.0) **before the tag** (§10 permanent-at-tag metadata). The **field-code-provisional disclosure is in the `.zenodo` description** (count-match, 91/131 codes, field_coarse = S/E/H only, ~40 residual NULL pending the TOD reference), not just the methods note. **Pre-tag gate suite PASS:** provenance==MANIFEST (all 8 harmonized parquets + all 11 NSF 25-317 GSS reference tables); A1b text-validity (zero NUL / LF-only / valid UTF-8 on every committed text blob, incl. the unicode-heavy v4 metadata); A1 determinism (the three GSS parquets rebuild to their exact pinned bytes — working tree clean). The annotated tag `v4.0.0-gss` is cut under the **one concept DOI 10.5281/zenodo.20404785** (datasets are versions, not new deposits). **The GitHub Release publish — the Zenodo mint — is the maintainer's one-way door and is NOT done here.**

- **2026-06-02 — FLAG for maintainer doctrine fix: CLAUDE.md §1 names the wrong source survey.** CLAUDE.md §1 describes the Federal S&E Support module as "drawn from the **Survey of Federal Funds for R&D**." The staged export's own `table-description-FSS.txt` and the data content show the source is the **Survey of Federal Science and Engineering Support to Universities, Colleges, and Nonprofit Institutions** (a distinct NCSES survey). Per maintainer decision (2026-06-01) the deposit-facing metadata (`.zenodo.json`, `CITATION.cff`, `README.md`) was corrected to the accurate survey name at the v3.0 re-base; **CLAUDE.md §1 is flagged here for a doctrine fix by the maintainer** (doctrine edits are the maintainer's, not silently applied by the build). No deposit harm — the deposit metadata is correct as of v3.0; only the in-repo doctrine line lags. **Second doctrine flag (maintainer's, 2026-06-02): CLAUDE.md §4 cross-survey-spine addendum (~line 134) is stale** — it says the FedSupport set in scope is "FY2020–FY2023 … entirely modern-era," but v3.0 re-bases to **FY1971–FY2023 with native UNITID throughout**; the HERD *join* is UNITID-clean only for HERD era-B (2010–2023), and pre-2010 HERD identity still awaits the HERD era-A / IPEDS cycle. Both §1 and §4 are maintainer doctrine edits to make after the v3.0 diff review.

- **2026-06-02 — Doctrine reconciliation: CLAUDE.md re-based to the shipped v3.0 reality (maintainer-authorized; RESOLVES the two 2026-06-02 §1/§4 flags above).** (Doctrine edit, maintainer-authorized this sprint — the diff was surfaced for review before commit, per CLAUDE.md §0/§9. Not an agent override; logged here so the reconciliation is durable and the prior flags are closed.)
  - *Reconciled:* **§1 Roadmap** — source-survey name corrected to the **Survey of Federal Science and Engineering Support to Universities, Colleges, and Nonprofit Institutions** (was "Survey of Federal Funds for R&D"); recorded the **v2.0** (FY2020–2023 Table 12 slice) → **v3.0** (full FY1971–2023 NCSES Build Table series) re-base; GSS blocker updated to **RESUMED 2026-06-02** (HD 4.1 the ready next action, dataset-#3 kickoff). **§1 Tier-1 bullet** GSS status synced (swept — it contradicted the updated Roadmap). **§3 acquisition-format lock** — removed the now-false "Federal S&E Support module = xlsx" example (the v3.0 source is **CSV-native**); kept the lock principle for any future xlsx-arriving source. **§4 spine addendum** — name-reconstruction → **native-UNITID-keyed join** (FY2023 dollar-join 78.4% vs the 73.1% name-spine; name layer retained **offer-only** for HERD era-B NULL-UNITID backfill, Seam-B-gated, deferred to IPEDS #4); replaced the stale "FedSupport set in scope FY2020–FY2023 … entirely modern-era" with the FY1971–2023 + HERD-side-constraint framing. **§10** tag example `v2.0.0` → `v3.0.0-fedsupport`.
  - *Authorization:* maintainer-directed; doctrine edits are the maintainer's and the CLAUDE.md diff was surfaced and approved before commit.
  - *Kill condition:* if any reconciled line still misdescribes the shipped v3.0 artifacts, it re-opens — the deposit metadata (`.zenodo.json` / `CITATION.cff` / `README.md`) is the ground truth and the doctrine line follows it, never the reverse.

- **2026-06-02 — HD 4.1 GSS gate-spike: VERDICT GO (dataset #3 build authorized to scope); + §3 acquisition-format DECISION (pattern-setting for all NCSES-microdata-zip surveys).** (Decision record, not an agent override — the ratified first-action gate per the 2026-05-31 dataset-#3 entry above. Full evidence: `docs/gss/hd_4_1_gate_findings.md`.)
  - *Gate verdict:* **GO** — both override-§12 kill conditions cleared on read-only evidence (stdlib XLSX probes, all 53 years FY1972–2024). **KC-1 (no clean identity key): NOT triggered** — GSS carries **native IPEDS UNITID on 100% of rows in every year** (no era-A gap, the decisive contrast with HERD), at **campus grain** (Texas A&M → 8 distinct campus UNITIDs, UC → 10), with a bounded `999999` no-match sentinel (≈1 inst). Two-number spine receipt vs the HERD+FedSupport spine: **97.0–99.1% institution-match, 92.8–99.7% count-weighted** across the span (a 1984–87 count dip to ~93% flagged for the build receipt). **KC-2 (non-decomposable discontinuity): NOT triggered** — race taxonomy is **pre-bridged by NCSES** (legacy `_98` + modern OMB-1997 columns run in parallel 1972–2016, legacy retired losslessly at 2017); the **2017 redesign is additive** (Race 122→290 cols: −30 legacy, +198 doctoral-`dr_` detail) over a continuous total-grad backbone; the Support re-coding at 2017 is a semantics-preserving crosswalk object; frame seams (1984–87 contraction, 2014 expansion) take the HERD-style cohort decomposition. **Thesis confirmed:** the Support sheet (mechanism × federal agency × fed/nonfed) is present and joinable across the full span and composes with HERD + FedSupport on the institution-year hub via the shared agency taxonomy (NSF/NIH/DOD/DOE/NASA/USDA) — GSS is the funding-of-human-capital third face.
  - *§3 acquisition-format DECISION (sets the pattern for every future NCSES SAS+XLSX-zip survey, incl. SED):* GSS arrives as `sas7bdat` + `xlsx` (no CSV); neither loads under the pinned runtime (`duckdb`+`pypdf` only — verified). **Decision: XLSX → CSV per sheet (Race/Support/PD_NFR) is AUTHORITATIVE, converted once at acquisition by a stdlib reader (xlsx = zip+XML, ZERO new dependency); the `sas7bdat` is kept as the provenance/audit sibling** (a valid 64-bit LE SAS file; exact SAS↔XLSX row reconciliation via `pyreadstat` is a one-time acquisition cross-check, NOT a build dep). This is **stronger than the FedSupport precedent** (which feared a SAS→`pyreadstat` build path) — runtime stays `duckdb`+`pypdf`. The XLSX is the published wide tabulation at our exact target grain (institution × field × demographic/support counts, UNITID-keyed), internally exact (3 sheets cross-foot: 1975 FT-total 219,648 = 219,648; 2008 = 449,613). v3.0 hygiene from the start: converted CSVs are generated → UTF-8/LF; the as-downloaded zips are provenance → `-text` + `data/raw/MANIFEST.md` SHA pins.
  - *B-first calibration (relates to the 2026-05-31 kill condition):* the requirements-discovery rationale **paid off modestly, not fully**. GSS is so identity-clean (campus-native UNITID) that it does **not stress** the system-vs-campus seam the override hypothesized — it **relieves** it (a campus-enumeration reference for decomposing FedSupport's system-level obligations). The genuine new requirement it surfaces is smaller: a PhD-granting **research-institute sub-universe** (Scripps/Mayo/Sloan Kettering) for the registry to absorb (19 GSS-only institutions in 2023), plus a few **`49xxxx`-block UNITIDs** confirming the UNITID-vintage/reassignment problem IPEDS #4 owns. So B-first did surface *a* requirement (the 2026-05-31 kill condition does not fire), but the discovery argument is **down-weighted** relative to the thesis argument, which is fully vindicated. Recorded for panel calibration.
  - *Kill condition (what would prove GO wrong):* if the build's full XLSX→CSV conversion surfaces a year whose XLSX is malformed/absent such that SAS becomes the only carrier (forcing `pyreadstat` onto the build path), the §3 XLSX-authoritative decision reopens; or if the 2017 support re-coding crosswalk proves to drop a federal-agency or mechanism distinction that the pre-2017 series carried (a genuine semantic loss, not a rename), the KC-2 decomposability verdict reopens for that axis.

## Calibration notes

Not overrides (no Maintainer-override-of-agent-verdict) — calibration evidence for periodic panel review, kept here alongside the override log per §12's "calibration happens at panel review" framing.

- **2026-06-01 — Record-keeping reconciliation: `seeds/overrides.md` (+ CLAUDE.md) IS the strategy record; no PANEL_VISION.md is owed.** Surfaced during the FedSupport re-base Phase-1 internal-record reconciliation (`docs/fedsupport_rebase_scope.md`). `PANEL_SKIPPER.md` is the *engineering* lens's relitigation-proof locked-decision ledger (its §8 is what CLAUDE.md §8 cross-references — a single ledger keeps engineering decisions out of the code and stable against re-litigation). Vision's output has two homes **by design**, so the apparent asymmetry is not a missing file: ratified strategic verdicts become **CLAUDE.md** locked doctrine, and overrides / divergences / calibration land **here** (this file — the 2026-05-31 GSS dataset-#3 *decision record* is the worked example). There is no orphaned class of Vision output that needs a third file; creating PANEL_VISION.md would split the strategy record across three places instead of consolidating it. **Disposition:** confirmed — overrides.md + CLAUDE.md is the strategy record. CLAUDE.md §10's loose gloss "PANEL_*.md — panel review outputs by topic" is accurate-enough (the panels are *engineering* topics); no doc edit forced. **Kill condition:** if a future class of Vision decision arises that is neither CLAUDE.md-doctrine nor an override/calibration entry (e.g. a standing strategic register that needs its own relitigation-proof ledger like Skipper's), revisit whether a PANEL_VISION.md earns its slot.

- **2026-05-29 — Forward dollar-floor mis-calibration (HD 3.2 spine).** The forward kill condition on the FedSupport identity spine set a **~80% dollar-match floor** (scope-doc §11 forward kill, PRIMARY) as an abstract coverage target. The floor *fired correctly* — the MVP came in at 73.1% dollar-match, the surface-to-Vision reflex triggered, and the surface revealed the genuine structural finding (HERD's era-B `ipeds_unitid` is canonical-not-complete; the gap is an IPEDS-cycle deliverable, not a converging manual tail). So the mechanism worked. But the *number* was mis-set: ~80% was chosen as a generic "good coverage" target, not derived from the join target's actual keyspace completeness. The spine's true ceiling is **~80.7% dollars** (the best-case manual ceiling, and only if every ambiguous flagship is hand-resolved to the correct campus UNITID), driven by HERD's own era-B UNITID coverage (94.3% of rows, 4.2% of distinct institutions NULL at source). A forward floor of ~80% against a join target whose UNITID-coverage ceiling is ~80.7% leaves a ~0.7-point window — the floor was effectively un-clearable by in-scope effort, which is *why* it fired, but the firing read as "the spine underperformed" when the accurate read is "the floor was set above the join target's structural ceiling." **Calibration:** forward coverage floors for cross-survey spines must be **derived from the join-target's actual keyspace completeness** (here, HERD's era-B UNITID coverage), not from a generic coverage target. This is the **third calibration finding this sprint** and the second of the mis-set-metric family — it mirrors the §11 kill-metric correction (which pinned the *axis*: dollar not institution, and the *pass*: post-mechanical not raw-first-pass); this one pins the *level*: the floor must be anchored to the target keyspace's ceiling, not asserted abstractly. The reflex is kept (an un-clearable floor that surfaces a real structural finding is the asymmetry working as designed); only the level-setting discipline is corrected. **Kill condition for the captured guidance:** if a future cross-survey spine's join-target keyspace completeness is genuinely unknowable at floor-setting time (no way to measure the target's key coverage before building the spine), then a generic floor is the only available instrument and this guidance does not apply — revisit whether "derive from target completeness" is always feasible or only when the target is already harmonized in-repo. No outcome harm; the MVP ships at its characterized ceiling per principal ratification.

- **2026-05-30 — Grain-tier figure corrected by generator-honesty (HD 3.2 receipt, Option A).** The HD 3.2 identity-spine receipt's grain tier (system-vs-campus flagship ambiguity, Seam B) was ratified at **8.2% / 16 institutions / 81.3% best-case ceiling** — a figure that cleared Vision's verdict, panel review, AND principal ratification. It was wrong. The number came from a throwaway recovery spike (since superseded by the build's own gate-evidence pass) counting candidate *name-variant rows* (`len(cands)`): an institution with multiple HERD name spellings that all resolve to ONE UNITID (e.g. IUPUI, 3 spellings → a single UNITID) was double-listed as grain-ambiguous. When the receipt generator was rebuilt under Option A (generator-as-source-of-truth, §3 reproducibility — the generator must emit the deposit artifact so a rebuild reproduces it), it was forced to derive the decomposition from the actual spine and counted *distinct UNITIDs* — the correct grain measure — yielding **7.6% / 10 / 80.7%**. The other two tiers reproduced exactly; the IUPUI distinct-UNITID resolution independently cross-validates the gate check-(a) 0.6%-recoverable finding (two counts agree). **Calibration / meta-lesson:** build-derived truth beat consensus-ratified assumption — a figure can clear verdict + review + ratification and still be wrong if it rests on a hand-computed counting choice no one re-derived from source. The defense that caught it was *structural, not vigilance*: making the generator the single source of truth surfaced the error **by construction** on the next rebuild, where three rounds of human review had not. This is the **fourth calibration finding this sprint** and the strongest evidence for the generator-as-source-of-truth discipline — a hand-curated receipt would have shipped 8.2%/81.3% silently into the deposit. SHIP-(A) holds a fortiori on the corrected figure (Vision re-affirmed 2026-05-30; the correction moves the same direction as the conclusion — gap marginally more structural, ceiling now only ~0.7pt above the floor). **Kill condition for the captured guidance:** if generator-emission of a narrative receipt ever ossifies framing that genuinely needs editorial revision across rebuilds (the generator becoming a barrier to legitimate prose improvement), revisit the all-generated posture toward a generator/hand-authored split for the editorial layer — but the HD 3.2 receipt's narration was fully data-derivable (zero un-templatable sentences surfaced), so the all-generated form was correct here. No outcome harm; the correct figure ships.

- **2026-05-30 — Narration direction-inversion caught by verbatim receipt read (HD 3.2, 5th calibration finding).** The HD 3.2 spine receipt's §1 narration opening stated the institution-vs-dollar divergence **backwards**: it read "the dollar rate sits *below* the institution rate — the unmatched tail is dollar-heavy, which is the direction §9 flags as dangerous," when the true direction is the reverse — dollar-match **73.1% sits ABOVE** institution-match **55.8%**, the matched set is the dollar-heavy set, so the unmatched tail is dollar-**light**, which is the **SAFE** direction. The sentence both inverted the comparison AND drew the dangerous-direction conclusion (the exact misread the clause-(c) receipt exists to prevent), and it **self-contradicted the very next sentence**, which correctly named the SAFE direction. It survived generator-emission, framing-authorship, the structural-summary review, AND the figure/structure ratification; it was caught only when the principal read the **verbatim** narration paragraph against the numbers, pre-push. Fixed in the generator's `_write_receipts` template (Option A — not a hand-edit), rebuilt byte-stable (the v1.0.0 gate artifact ships correct from the start). **Calibration / meta-lesson (the sharpened one):** the cold-reader-facing **prose is itself a summary-maskable artifact**. The figures were right and the structure was right; a structural summary confirmed both and nearly closed the gate — but the *sentence about* the numbers was false. Prose is a **separate correctness surface** from the figures it describes, and it passed emission + framing-authorship + summary-review on a sentence that contradicted its own data. This is the latest numbers-vs-narration mismatch the read-the-actual-artifact discipline has surfaced this sprint (after the §11 kill-metric axis/pass correction, the floor mis-calibration, and the grain-counting quirk) — the fifth calibration finding overall, and the fourth caught by reading the artifact rather than its summary. **Every future deposit has a narration layer, and every narration layer is summary-maskable.** The locked discipline: **structural summaries confirm structure; only verbatim reads confirm the words are true** — the narration paragraph of any deposit-facing artifact must be read as text, against the numbers, before the artifact gates a release. **Kill condition for the captured guidance:** if verbatim narration reads add no catches beyond structural review across the next several deposit artifacts, relax to spot-reads — but four read-the-artifact catches in one sprint argues the other way. No outcome harm; caught pre-push.

- **2026-05-30 — Byte-stability discipline must cover EVERY generated deposit artifact, not just the one under suspicion (HD 3.2 spine, 6th calibration finding).** A §3 reproducibility defect was caught pre-push: the FedSupport identity spine CSV (`crosswalks/_shared/institution_identity.csv`) was **non-deterministic** — two consecutive `build_fedsupport_identity_spine.py` runs produced different SHA-256 (F5FBB29… vs 1427277…). The cause: when ≥2 HERD era-B display names map to one matched UNITID, the `herd_inst_name_long` alias was picked by unordered DuckDB `SELECT DISTINCT` iteration (`uid_to_name.setdefault`), so the manufactured display name flapped run-to-run (~123 of 1,506 rows on those two runs; 203 UNITIDs structurally at risk). UNITID, match_method, rates (55.8%/73.1%), and row order were stable — **only the human-readable alias flapped**, and a committed deposit artifact that must rebuild bit-equivalently per §3 had shipped into the commit. Fixed with a documented tiebreak (HERD era-B row-frequency then alphabetical, stated in the affected rows' `decision_rationale` — a manufactured display value ships with its rule, like every other manufactured value in this project); rebuilt byte-stable (df74f9bb… across three consecutive runs). **The meta-lesson — and why this is a calibration finding, not just a bug fix:** the receipt (`identity_spine_match_rate.md`) got the byte-stability (Option-A two-build) check *because we were actively editing it*; the spine CSV — **equally generator-emitted, equally a deposit artifact** — did not get the check because it was "done" and was never re-run-and-compared. Non-determinism sits dormant in a finished artifact precisely because finished artifacts stop being rebuilt. **The generalized discipline: before any deposit ships, ENUMERATE every generator-emitted artifact in the deposit set and run the two-build SHA gate on EACH — not only the one under active edit.** A clean sweep was run on the full FedSupport generated set (spine CSV: stable after fix; spine receipt: stable; obligations parquet: byte- AND data-stable, not even invoking the §3 writer carve-out); `type_of_activity_map.csv` and `fedsupport_parse_reconciliation.md` are hand-authored and out of the gate by definition. This is the **sixth calibration finding this sprint** and the first in the reproducibility-determinism family (the prior five were metric mis-calibration ×2 and numbers-vs-narration ×3). The fix is also promoted into `docs/release_runbook.md` as a hard named pre-deposit gate (the finding has forward leverage — it must fire on GSS/SED/IPEDS too, not only FedSupport). **Kill condition for the captured guidance:** if a future generated artifact is provably deterministic by construction (e.g., a pure passthrough of an already-hashed input with no manufactured/aggregated values and no unordered iteration), the two-build SHA is redundant for it — but the cost of running it is ~one rebuild, so the gate stays mandatory-by-default and the exemption must be argued per artifact, not assumed. No outcome harm; caught pre-push, v1.0.0 held.

- **2026-06-01 — (f) coverage-cliff was a UNIVERSE ARTIFACT (FedSupport re-base, caught by independent recompute).** The Phase-2 scope memo's (f) reported a "1971–1998 ≈ 65% dollar coverage, cliff at 1998/99" UNITID-coverage discontinuity and framed it as an NCSES back-assignment vintage. **It was wrong.** The (f) by-year probe filtered the universe with `itype LIKE 'Academic%'`, which swept in **Academic FFRDC** rows — a *separate* institution type whose UNITID is the `na_ffrdc` sentinel by design. FFRDC carried ~30% of academic+FFRDC dollars in the 1970s–80s and drops out of the survey's academic universe after FY1998; including it in the denominator manufactured both the early-era "65%" and the "1998/99 cliff." On the **join universe** (academic+consortium, FFRDC excluded — the same universe (b)/(c) used), early-era coverage is **~92–97%**, flat across 53 years; the only genuine steps are modest (1993→94 up; 2008→09 and 2015→16 down). The "1998/99 cliff" is the **FFRDC reporting boundary**, relocated to the FFRDC universe as its own RH clause-(b) object (`validation/reports/fedsupport/coverage_discontinuity.md` §3). **Why it's a calibration finding, not just a bug:** it is the same family as the grain-tier (8.2%→7.6%) and floor (~80%→80.7%) mis-calibrations — a figure that cleared the scope-memo pass but rested on the **wrong denominator**, and was caught **structurally** (an independent RFC-4180 recompute on the correct universe), not by re-reading. The (c) probe escaped the bug only because it reported FY2020–2023, where Academic FFRDC is already ~0. **Locked discipline:** a coverage/denominator figure that spans a universe with by-design-excluded sub-populations (FFRDCs, nonprofits) must state its universe filter explicitly and be recomputed on the *join* universe before it reaches a methods note or permanent DOI metadata. **Kill condition:** if the join-universe recompute itself proves universe-sensitive in a way the explicit filter cannot capture, revisit whether "filter by institution_type" is the right universe primitive. No deposit harm — caught before the methods note and the v3.0.0 tag, the carve-out the maintainer imposed.

- **2026-06-02 — Build-surfaced correction: native UNITID does NOT cleanly backfill the HERD-NULL giants (they are Seam-B). REINFORCES IPEDS #4.** The Phase-2 memo (c) implied the FedSupport native UNITID could backfill HERD's era-B NULL-UNITID giants (e.g. "Johns Hopkins → 162928"), partially pre-empting dataset-#4 IPEDS. The v3.0 spine build shows otherwise: the **dollar-dominant** HERD-NULL institutions — Johns Hopkins, Ohio State, Texas A&M (College Station), Cincinnati, Connecticut — are **multi-campus on the FedSupport side too** (`Johns Hopkins U.` resolves to *two* native UNITIDs), so they land in the **Seam-B / system-vs-campus** bucket and are UNOFFERED (no forced campus pick). The clean single-candidate backfill offers are smaller institutions worth only **+1.0%** of the FY2023 anchor. So the native UNITID buys the FedSupport-side identity (78.4% join, up from 73.1%) but the giants still need the IPEDS #4 **system→campus hierarchy** — which **reinforces** IPEDS #4's rationale rather than weakening it (revising the Phase-2 roadmap read in the other direction). Caught by reading the actual `herd_unitid_backfill_offer.csv` against the receipt narrative — the receipt's draft "FedSupport supplies those UNITIDs (Johns Hopkins → 162928)" sentence was numbers-vs-narration-false and was corrected in the generator before ship (same family as the 5th calibration finding). **Kill condition:** if the IPEDS #4 cycle finds the Seam-B giants are in fact single-campus (the multi-UNITID FedSupport rows are duplicates, not distinct campuses), the backfill is cleaner than this build concluded and IPEDS #4's grain-resolution value shrinks accordingly.

- **2026-06-02 — Gate gap: reproducible ≠ valid (FedSupport v3.0, caught at pre-tag).** A deposit text artifact (`validation/reports/fedsupport/identity_spine_match_rate.md`) was caught at the pre-tag check as **UTF-16/NUL-corrupt** — 8,257 NUL bytes in 13,413; git read it as binary. The numbers were correct; the *encoding* was not. **The determinism gate (A1) had passed it** — because byte-stability only proves an artifact is *reproducibly* generated, not that the bytes are *valid text*. A deterministically-corrupt file sails straight through a byte-stability check. Root cause: a text-mode/redirect write produced UTF-16 (and, separately, the whole generated set was CRLF from Windows Python text-mode — a latent cross-OS §3 SHA break, since the same generator would emit LF on Linux). **Why it's a calibration finding:** it is a NEW gap class — the prior determinism discipline (6th calibration finding) asked "does it rebuild identically?" and this one adds "are the rebuilt bytes valid?" The two are orthogonal; A1 cannot catch A1b's failure. **Fix:** (1) generators write raw UTF-8/LF bytes with a read-back NUL/UTF-8/CR assertion (`etl/_load_fedsupport.write_text_clean`), so corruption fails the build at write time; (2) a deposit-wide **A1b text-validity sweep** added to `docs/release_runbook.md` (sibling to A1); (3) a committed `.gitattributes` (`* text=auto eol=lf`, `*.parquet binary`) keeps line-ending churn out of the deposit. **Kill condition for the captured guidance:** if a future deposit legitimately needs a non-UTF-8 or non-LF text artifact (e.g. a fixture that must carry CRLF), the sweep must allow a per-file opt-out rather than a blanket assert — revisit the blanket rule then. No deposit harm — caught at pre-tag, before the v3.0.0 tag and the permanent DOI.
  - **Backlog scope + disposition (addendum 2026-06-02).** The repo-wide A1b sweep showed the v3.0 receipt was not a one-off but the latest instance of a **~15-file pre-existing EOL/encoding backlog dating to the HERD era** — ~11 spike `etl/spikes/*_output.txt` with UTF-16/NUL (PowerShell `>`/Out-File default capture) + several CRLF deposit artifacts (`era_reconciliation_*.md`, `crosswalks/_harvest/*.csv`, `data/reference/**/*.csv`, `herd_fy24_guide.txt`). It is a backlog the sweep *surfaced*, **not a v3.0 regression** (the corrupt files are already inside the v1.0.0/v2.0.0 tags), so it does **not** gate the v3.0 tag. Disposition: the **6 spike outputs CITED in deposit docs** (`stage_4_smoke_test_output`, `probe_1978_1988_drops_output`, `probe_era_b_status_u_full_output`, `spike_qno_suffix_semantics_output`, `probe_status_c_codeset_output`, `spike_equipment_era_boundary_output`) were **re-saved to UTF-8/LF before the v3.0 stage** (kept tracked — they are preserved audit evidence, NOT gitignored; 1–3 pre-existing-lossy decorative em-dashes render as U+FFFD, data fully intact). The remaining uncited corrupted outputs + CRLF normalization are deferred to a **dedicated post-tag normalization commit** (root-cause kill: every generator now emits UTF-8/LF explicitly via `write_text_clean`). This mirrors the 6th calibration finding (the "enumerate EVERY generated artifact" determinism discipline) one layer up: enumerate every artifact for *validity*, not just *stability*.
  - **Refinement (2026-06-02): gate the INDEX/COMMITTED blob, not the working tree — and NOT `git diff --cached --numstat`.** Pre-tag verification flagged 5 cited spike outputs as `-  -` (binary) under `git diff --cached --numstat` and inferred the re-save "wasn't git-add'ed, staged blobs still corrupt." The inference was wrong: `git cat-file -p :path` showed the **staged blobs were clean (NUL=0) all along** — the first pass *did* stage them correctly. `numstat --cached` diffs INDEX-vs-HEAD and prints `-  -` if *either side* is binary; the **HEAD** blobs were still UTF-16/NUL, so numstat reported `-  -` for clean staged blobs. Proof: after a verified-clean re-save+add, numstat (even `--text`) STILL showed `-  -`, while `git cat-file -p :path` confirmed zero NUL. So numstat is the **wrong gate** (false-corrupt on clean staged blobs; only goes numeric post-commit). **The lesson the reviewer reached for is exactly right — verify the bytes the tag captures — but the correct instrument is `git cat-file -p :path` on the index blob, not the working tree (A1b's first gap) and not numstat-vs-HEAD (confounded by binary HEAD).** Added to the release-runbook A1b gate. Belt-and-suspenders this pass: ALL 14 corrupt spike `*_output.txt` (cited + uncited) were re-saved to UTF-8/LF and `git add`-ed (`hd_2_4_a` was already clean at HEAD — a working-tree-only CRLF artifact), retiring the post-tag spike-output cleanup; only the CRLF reports/CSVs (`era_reconciliation_*.md`, `crosswalks/_harvest/*.csv`, `data/reference/**`) remain for their own normalization commit.
  - **Normalization commit LANDED (2026-06-02) — and the provenance half was the INVERSE of the expected bug.** The deferred CRLF normalization shipped as its own commit. Two findings, both surfaced by gating the **INDEX blob** (`git ls-files --eol` / `git cat-file`) not the working tree (the mount's CRLF view is unreliable — explicitly the discipline the 3rd A1b refinement above pinned): (1) **the project-authored "CRLF backlog" was already clean at the committed-blob level.** `* text=auto eol=lf` (added at v3.0) had already made EVERY committed text blob LF — `era_reconciliation_*.md`, `crosswalks/_harvest/era_a|b_row_labels.csv`, `encoding_substitutions.csv` were all `i/lf`; the CRLF was **working-tree/mount-only** and never entered a commit, so no LF re-commit was owed. The forward fix is **root-cause at the generators**: 7 HERD-era generators emitted CRLF via Windows text-mode / csv's default `\r\n` (`harvest_era_a_row_labels.py`, `harvest_era_b_row_labels.py`, `_load.py` encoding log, `national_totals_2008_2011.py`, `residual_2008_2011.py`, `residual_2008_2011_diagnostics.py`, `extract_herd_fy24_guide.py`) — all pinned to LF (`newline="\n"` / `lineterminator="\n"`), the HERD-era complement to `_load_fedsupport.write_text_clean`, closing the cross-OS working-tree/hash divergence at the source. (2) **The genuine bug was inverted from the brief's premise** ("leave the committed provenance bytes bit-exact"): two externally-sourced exports (`data/reference/dst-table-builder/dst-table-builder-FY2024.csv`, `precondition_metadata.txt`) were committed **LF** but their `data/reference/MANIFEST.md` SHA-256 pins the **as-downloaded CRLF** (3,472 / 600 B; the byte gap == the stripped CR count, and a lossless LF→CRLF reconstruction reproduces the pin bit-exactly). The blanket `eol=lf` had silently LF-stripped the *tracked* provenance copy out of agreement with its own hash — a §3 break where *re-fetch-from-URL-and-verify* still passed but *checkout-and-verify* failed on Linux. Fixed with a `.gitattributes` `-text` per-file opt-out (`data/reference/dst-table-builder/*.csv|*.txt`) + `git add --renormalize` restoring the CRLF bytes; the staged-blob SHA now reproduces the MANIFEST pin exactly, **no MANIFEST edit** (the pin was right all along; the tracked bytes were wrong). **Meta-lesson:** `* text=auto eol=lf` is correct for project-authored artifacts and **wrong for byte-pinned external provenance** — the latter needs the `-text` opt-out, which is exactly the per-file opt-out named in this entry's original kill condition; this commit is its first exercise. The HERD-era spike `*_output.txt` debt (item (i)) was already retired at the v3.0 stage (all 14 re-saved to UTF-8/LF and added), so the repo-wide A1b sweep is green after this commit except the two intentional `-text` provenance files. **Kill condition for the captured guidance:** if a future externally-sourced artifact arrives LF-native with an LF MANIFEST pin, no `-text` opt-out is owed (the blanket rule already holds) — scope the opt-out per provenance file by its as-downloaded EOL, never by blanket directory, if a reference dir ever mixes LF-native and CRLF-native exports.

- **2026-05-26 — De-staling judgment near-miss (Stage 3 Phase C).** During the Phase C DOI swap, Skipper expanded beyond the literal `XXXXXXX → DOI` string replacement to de-stale the surrounding "reserved / minted-at-release" framing (README, CITATION.cff, BibTeX note, LICENSE-DATA.md) that a literal-only swap would have left false post-mint. Skipper applied the expansion and surfaced it transparently at completion; Vision (step-5 assessment) endorsed it as methodologically correct ("would have approved on sight") and coherent. **Calibration:** the judgment was right, but under the 7-step pattern a framing-semantics expansion ideally routes to Vision *before* application — transparent-flag-at-completion is the floor, Vision-surface-before-application the ceiling. Captured durably in `memory/feedback_scope_expansion_vision_surface.md`. No outcome harm. Kill condition for the captured guidance: if surface-before-application proves to add material latency on time-critical inline fixes without a quality benefit, revisit the floor/ceiling framing.
